# Cutting in Tesseract

Moved to its own skill: **`tesseract-edit`** (`~/.claude/skills/tesseract-edit`).

It holds the workflow, the `tsx.py` layout library, per-creator caption presets, the feed swipe,
green-screen cut-ins, ducking, delivery, verification, and the fourteen tsrct 0.1.0 traps where
Mirage's docs are wrong (`references/traps.md`). The narrator's animated text is `kinetic-type`.

Use Tesseract for anything with structure (a promo, a compilation, a cast reel you will revise).
Keep `scripts/edit_hook.py` for a single hook with burned-in captions.
